import { Container, Section } from '@/components/ui/Section'

/** TODO(task): replace this stub with the real section (see the plan). */
export function BlogsPreview() {
  return (
    <Section id="blogs">
      <Container>
        <p className="col-span-2 px-5 text-mono text-gray-500">BlogsPreview (stub)</p>
      </Container>
    </Section>
  )
}
