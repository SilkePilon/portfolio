import { Container, Section } from '@/components/ui/Section'

/** TODO(task): replace this stub with the real section (see the plan). */
export function About() {
  return (
    <Section id="about">
      <Container>
        <p className="col-span-2 px-5 text-mono text-gray-500">About (stub)</p>
      </Container>
    </Section>
  )
}
